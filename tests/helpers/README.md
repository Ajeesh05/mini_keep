These files are synced from `ext-automation/tools/`. Do not edit them here -
change the orchestrator copy and let `sync-agent-config.yml` push the update,
or the next sync will overwrite your change.
